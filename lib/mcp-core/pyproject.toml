[project]
name = "mcp-core"
version = "0.1.0"
description = "Shared core logic for MCP servers"
requires-python = ">=3.14"
dependencies = [
    "feedparser==6.0.12",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
