[project]
name = "app-core"
version = "0.1.0"
description = "Shared core business logic for app services"
requires-python = ">=3.14"
dependencies = [
    "feedparser==6.0.12",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
