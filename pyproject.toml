[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "unified-mcp-app"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi==0.136.3",
    "fastapi-mcp==0.4.0",
    "fastmcp==3.4.2",
    "feedparser==6.0.12",
    "uvicorn==0.49.0",
]
