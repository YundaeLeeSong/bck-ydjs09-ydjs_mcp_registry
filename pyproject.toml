[project]
name = "ydjs-mcp-registry"
readme = "README.md"

[tool.uv.workspace]
members = ["app/*", "server/*", "lib/*"]

[tool.uv]
dev-dependencies = ["poethepoet"]

[tool.poe.tasks]
sync-deps = "uv run uv/pipreqs.uv.py"
